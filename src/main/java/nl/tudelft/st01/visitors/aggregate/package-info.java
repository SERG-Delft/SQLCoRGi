/**
 * This package contains visitors used to generate mutations for aggregate functions (MIN, MAX, SUM, AVG, COUNT) and
 * framed relations (GROUP BY).
 */
package nl.tudelft.st01.visitors.aggregate;
